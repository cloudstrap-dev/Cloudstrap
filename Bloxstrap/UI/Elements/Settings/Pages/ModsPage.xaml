<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.ModsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:ModsViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="ModsPage"
      Scrollable="True">

    <ui:UiPage.Resources>
        <dmodels:FilePathToImageConverter x:Key="FilePathToImageConverter" />
    </ui:UiPage.Resources>

    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <Grid Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:CardAction Grid.Row="0" Grid.Column="0" x:Name="OpenModFolderCardAction" Margin="0,0,4,0" Icon="Folder24" Command="{Binding OpenModsFolderCommand}">
                <StackPanel>
                    <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Mods_OpenModsFolder_Title}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Text="{x:Static resources:Strings.Menu_Mods_OpenModsFolder_Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Row="0" Grid.Column="1" Margin="4,0,0,0" Icon="BookQuestionMark24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://bloxstraplabs.com/wiki/features/modding/">
                <StackPanel>
                    <TextBlock FontSize="14" Text="{x:Static resources:Strings.Common_Help}" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Text="{x:Static resources:Strings.Menu_Mods_Help_Description}" Padding="0,0,16,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0" Icon="WindowWrench24" Command="{Binding OpenCompatSettingsCommand}">
                <StackPanel>
                    <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Mods_Misc_CompatibilitySettings_Title}" TextWrapping="Wrap" />
                    <controls:MarkdownTextBlock Margin="0,2,0,0" FontSize="12" MarkdownText="{Binding Source={x:Static resources:Strings.Menu_Mods_Misc_CompatibilitySettings_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://devblogs.microsoft.com/directx/demystifying-full-screen-optimizations/'}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>
        </Grid>

        <TextBlock Text="{x:Static resources:Strings.Common_Presets}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Presets_MouseCursor_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Presets_MouseCursor_Description}">
            <ComboBox Margin="5,0,0,0" Padding="10,5,10,5" Width="200"
                      ItemsSource="{Binding CursorTypeTask.Selections, Mode=OneTime}"
                      SelectedItem="{Binding CursorTypeSelection, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Presets_OldAvatarEditor_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Presets_OldAvatarEditor_Description}">
            <ui:ToggleSwitch IsChecked="{Binding OldAvatarBackgroundTask.NewState, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Presets_OldCharacterSounds_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Presets_OldCharacterSounds_Description}">
            <ui:ToggleSwitch IsChecked="{Binding OldCharacterSoundsTask.NewState, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Presets_EmojiType_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Presets_EmojiType_Description}">
            <ComboBox Margin="5,0,0,0" Padding="10,5,10,5" Width="220" ItemsSource="{Binding EmojiFontTask.Selections, Mode=OneTime}" Text="{Binding EmojiFontTask.NewState, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </controls:OptionControl>

        <TextBlock Text="{x:Static resources:Strings.Common_Miscellaneous}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Misc_CustomFont_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Misc_CustomFont_Description}">
            <StackPanel>
                <ui:Button Icon="DocumentAdd16" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomFont_Choose}" Command="{Binding ManageCustomFontCommand}" Visibility="{Binding ChooseCustomFontVisibility, Mode=OneWay}" />
                <ui:Button Icon="Delete16" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomFont_Remove}" Appearance="Danger" Command="{Binding ManageCustomFontCommand}" Visibility="{Binding DeleteCustomFontVisibility, Mode=OneWay}" />
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Mods_Misc_CustomDeathSound_Title}"
            Description="{x:Static resources:Strings.Menu_Mods_Misc_CustomDeathSound_Description}">
            <StackPanel>
                <ui:Button
                    Command="{Binding AddCustomDeathSoundCommand}"
                    Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomDeathSound_Choose}"
                    Icon="DocumentAdd16"
                    Visibility="{Binding ChooseCustomDeathSoundVisibility}" />
                <ui:Button
                    Appearance="Danger"
                    Command="{Binding RemoveCustomDeathSoundCommand}"
                    Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomDeathSound_Remove}"
                    Icon="Delete16"
                    Visibility="{Binding DeleteCustomDeathSoundVisibility}" />
            </StackPanel>
        </controls:OptionControl>

        <Expander Margin="0,10,0,0" Padding="16" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <Expander.Header>
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Title}" />
                    <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Expander.Header>

            <StackPanel Margin="0,10,0,0">
                <TextBlock Margin="0,0,0,12" FontSize="12" TextWrapping="Wrap"
                           Foreground="{DynamicResource SystemFillColorCautionBrush}"
                           Visibility="{Binding IsCursorPresetActive, Converter={StaticResource BooleanToVisibilityConverter}}">

                </TextBlock>

                <UniformGrid Columns="4">
                    <StackPanel Margin="4">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_ShiftLock}" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.7" />
                        <Border Height="100" CornerRadius="8" ClipToBounds="True" Cursor="Hand" Background="#2B2B2B">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShiftlockSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsShiftlockSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectShiftlockSlotCommand}" />
                            </Border.InputBindings>
                            <Image Source="{Binding ShiftlockCursorPreview, Converter={StaticResource FilePathToImageConverter}}"
                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                        </Border>
                        <ui:Button Margin="0,8,0,0" Icon="Folder24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Browse}"
                                   HorizontalAlignment="Stretch" Command="{Binding AddCustomShiftlockCursorCommand}"
                                   IsEnabled="{Binding CursorBrowseIsEnabled}" />
                    </StackPanel>

                    <StackPanel Margin="4">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Arrow}" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.7" />
                        <Border Height="100" CornerRadius="8" ClipToBounds="True" Cursor="Hand" Background="#2B2B2B">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsArrowSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsArrowSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectArrowSlotCommand}" />
                            </Border.InputBindings>
                            <Image Source="{Binding ArrowCursorPreview, Converter={StaticResource FilePathToImageConverter}}"
                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                        </Border>
                        <ui:Button Margin="0,8,0,0" Icon="Folder24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Browse}"
                                   HorizontalAlignment="Stretch" Command="{Binding AddCustomCursorCommand}"
                                   IsEnabled="{Binding CursorBrowseIsEnabled}" />
                    </StackPanel>

                    <StackPanel Margin="4">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_ArrowFar}" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.7" />
                        <Border Height="100" CornerRadius="8" ClipToBounds="True" Cursor="Hand" Background="#2B2B2B">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsArrowFarSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsArrowFarSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectArrowFarSlotCommand}" />
                            </Border.InputBindings>
                            <Image Source="{Binding ArrowFarCursorPreview, Converter={StaticResource FilePathToImageConverter}}"
                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                        </Border>
                        <ui:Button Margin="0,8,0,0" Icon="Folder24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Browse}"
                                   HorizontalAlignment="Stretch" Command="{Binding AddCustomArrowFarCursorCommand}"
                                   IsEnabled="{Binding CursorBrowseIsEnabled}" />
                    </StackPanel>

                    <StackPanel Margin="4">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_IBeam}" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.7" />
                        <Border Height="100" CornerRadius="8" ClipToBounds="True" Cursor="Hand" Background="#2B2B2B">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsIBeamSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsIBeamSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectIBeamSlotCommand}" />
                            </Border.InputBindings>
                            <Image Source="{Binding IBeamCursorPreview, Converter={StaticResource FilePathToImageConverter}}"
                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                        </Border>
                        <ui:Button Margin="0,8,0,0" Icon="Folder24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Browse}"
                                   HorizontalAlignment="Stretch" Command="{Binding AddCustomIBeamCursorCommand}"
                                   IsEnabled="{Binding CursorBrowseIsEnabled}" />
                    </StackPanel>
                </UniformGrid>

                <Grid Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <ui:Button Icon="Delete24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Remove}" Appearance="Danger" Margin="0,0,8,0"
                                   Command="{Binding RemoveSelectedCursorCommand}"
                                   IsEnabled="{Binding CanRemoveSelected}" />
                        <ui:Button Icon="DismissCircle24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_RemoveAll}" Appearance="Danger"
                                   Command="{Binding RemoveAllCursorsCommand}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Icon="FolderArrowUp24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Import}" Margin="0,0,8,0"
                                   Command="{Binding ImportCursorSetCommand}" />
                        <ui:Button Icon="ArrowExportLtr24" Content="{x:Static resources:Strings.Menu_Mods_Misc_CustomCursorFeatures_Export}"
                                   Command="{Binding ExportCursorSetCommand}" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Expander>
    </StackPanel>
</ui:UiPage>